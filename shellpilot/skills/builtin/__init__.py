"""Builtin skill root — empty package marker.

Skill folders live here as subdirectories (e.g. ``planning/SKILL.md``).
The root is empty until the planning skill lands in the next task.
Resolved via ``importlib.resources.files("shellpilot.skills.builtin")``.
"""
