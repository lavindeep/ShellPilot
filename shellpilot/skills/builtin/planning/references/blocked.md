When a command fails in a way that invalidates the plan, a file or API does not exist, or the same recovery fails twice, stop pushing that path.

Record blockers with update_plan(blocker="<evidence>") before revising the plan or asking one short, specific question.
