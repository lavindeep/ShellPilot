# Trigger Writing

Choose the narrowest trigger that matches when the model should see the skill. Use ENABLED for opt-in user skills, ALWAYS_ON only for tiny universal guidance, WEB_ENABLED for web-specific guidance, and plan triggers for plan-mode guidance.
